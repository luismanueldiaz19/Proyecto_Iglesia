import 'package:flutter/material.dart';

class ChurchColors {
  ChurchColors._();

  // Color principal
  static const Color primary = Color(0xFF0B2E6B);

  // Azul secundario
  static const Color secondary = Color(0xFF1E5AA8);

  // Dorado principal
  static const Color gold = Color(0xFFD4A017);

  // Dorado claro
  static const Color goldLight = Color(0xFFF0D37A);

  // Fondo claro
  static const Color background = Color(0xFFF0F2F5);

  // Blanco
  static const Color white = Color(0xFFFFFFFF);

  // Gris para texto secundario
  static const Color grey = Color(0xFF6B7280);

  // Gris muy claro
  static const Color lightGrey = Color(0xFFE5E7EB);

  // Negro para texto
  static const Color black = Color(0xFF1F2937);
}
